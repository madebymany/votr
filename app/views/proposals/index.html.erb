<div class="padded">
  <h1>Speaking Proposals</h1>

  <p>This application allows you to vote for the speakers that you want to hear at the Front-end London One Day event. There are 8 speaker slots for the day and you get one vote per slot.</p>

  <% if !voting_open? %>
    <p><strong>Voting will open on the 28th August</strong>.</p>
    <p>If you'd like to propose a talk, please <%= mail_to "frontendlondon@madebymany.co.uk", "get in touch with us", class: "red" %>.</p>

  <% elsif voting_open? && !voting_closed? %>

    <p><strong>Voting is now open and will close on the 13th September</strong>.</p>

  <% else %>
    <p><strong>Voting has now ended</strong>.<br>The following proposals will be
       delivered at the Front-end London One Day event.</p>
  <% end %>

  <%= link_to "Your votes", votes_proposals_path, class: "red" if current_user %>
</div>

<%= render @proposals %>
